{
  "name": "@marque/policy",
  "version": "0.0.1",
  "description": "Scope grammar and capability matching",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0"
}
