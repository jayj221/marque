{
  "name": "marque",
  "private": true,
  "version": "0.0.0",
  "description": "Identity and delegated permissions for AI agents",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "author": "jayj221"
}
